{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "My astonishing Nuxt.js project",
  "author": "akutagawa.junki",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate"
    "deploy": "push-dir --dir~dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "nuxt": "^2.0.0",
    "cross-env": "^5.2.0",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.0.0",
    "nodemon": "^1.18.9",
    "push-dir": "^0.4.1"
  }
}
